openapi: 3.2.0
info:
  title: Users User Roles API
  description: The remberg Users customer API description
  version: v1
  contact: {}
servers:
- url: https://api.remberg.de
tags:
- name: User Roles
paths:
  /v1/users/roles:
    get:
      description: Returns all user roles available in the tenant.
      operationId: /v1/users/roles_get
      parameters: []
      responses:
        '200':
          description: List of user roles
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UserRolesCfaFindManyResponseDto'
        '403':
          description: Forbidden
      security:
      - authorization: []
      summary: Get a list of assignable user roles
      tags:
      - User Roles
      x-controller-class: UserRolesCfaController
components:
  schemas:
    UserRolesCfaFindManyResponseDto:
      type: object
      properties:
        data:
          type: array
          items:
            $ref: '#/components/schemas/UserRoleCfaResponseDto'
      required:
      - data
    UserRoleCfaResponseDto:
      type: object
      properties:
        id:
          type: string
          description: A remberg internal unique identifier for the role.
        name:
          type: string
          description: The name of the role.
      required:
      - id
      - name
  securitySchemes:
    authorization:
      type: apiKey
      in: header
      name: authorization
